{
  "name": "appcoaching-website",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "react-scripts": "5.0.0",
    "gray-matter": "^4.0.0"
  },
  "scripts": {
    "generate-manifest": "node scripts/publish-blog.js --manifest-only",
    "publish-blog": "node scripts/publish-blog.js",
    "prebuild": "node scripts/publish-blog.js --manifest-only",
    "start": "node scripts/publish-blog.js --manifest-only && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
